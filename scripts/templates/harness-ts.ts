import { describe, it } from 'vitest';

/**
 * Harness: {{TITLE}}
 *
 * Purpose:
 * [Describe the business boundary or invariant this harness locks.]
 *
 * Related spec: {{RELATED_SPEC}}
 */

describe('{{TITLE}}', () => {
  it('should [expected behavior]', () => {
    // TODO: Replace this draft harness with real inputs and expected outputs.
    throw new Error('Replace this draft harness with real assertions before counting it as coverage.');
  });
});
