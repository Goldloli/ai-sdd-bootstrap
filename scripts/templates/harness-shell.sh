#!/usr/bin/env bash
# Harness: {{TITLE}}
# Purpose: {{PURPOSE}}
# Related spec: [Link to docs/feature/<feature-name>.md if applicable.]

set -euo pipefail

echo "Running harness: {{TITLE}}"

# TODO: Replace this draft harness with real setup, command, and assertions.
# Example:
# output=$(some-command)
# if [[ "$output" != *"expected"* ]]; then
#   echo "FAIL: expected 'expected' in output, got: $output"
#   exit 1
# fi

echo "FAIL: replace this draft harness with real assertions before counting it as coverage."
exit 1
